# Production dependencies — pin versions for reproducible builds.
# Update by running: pip freeze | grep -iE '^(pkg)=' inside a working container.
fastmcp>=2.0.0,<3.0.0
uvicorn>=0.30.0,<1.0.0
isilon-sdk>=0.7.0,<1.0.0
ansible-core>=2.16.0,<3.0.0
ansible-runner>=2.4.0,<3.0.0
jinja2>=3.1.0,<4.0.0
packaging>=24.0
pyyaml>=6.0,<7.0
starlette>=0.27.0
