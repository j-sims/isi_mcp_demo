apiVersion: v1
kind: Service
metadata:
  name: isi-mcp-backend
  namespace: isi-mcp
  labels:
    app.kubernetes.io/name: isi-mcp
    app.kubernetes.io/component: mcp-server
spec:
  selector:
    app: isi-mcp
  type: ClusterIP
  ports:
    - name: http
      port: 8000
      targetPort: 8000
      protocol: TCP
