# Copy this file to .env and adjust values for your deployment.
# .env is loaded automatically by docker-compose for variable substitution.

# Port exposed on the host for the nginx frontend (HTTPS)
PORT=443
